import { ElementRef, NgZone, Renderer, ComponentResolver, ViewContainerRef } from '@angular/core';
import { App } from '../app/app';
import { Config } from '../../config/config';
import { Keyboard } from '../../util/keyboard';
import { NavController } from './nav-controller';
import { ViewController } from './view-controller';
/**
 * @private
 */
export declare class NavPortal extends NavController {
    constructor(viewCtrl: ViewController, parent: NavController, app: App, config: Config, keyboard: Keyboard, elementRef: ElementRef, zone: NgZone, renderer: Renderer, compiler: ComponentResolver, viewPort: ViewContainerRef);
}
